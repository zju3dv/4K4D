sampler_cfg: &sampler_cfg
    ibr_regressor_cfg:
        resd_limit: 1.0

model_cfg:
    sampler_cfg:
        <<: *sampler_cfg
        fg_sampler_cfg:
            <<: *sampler_cfg
        bg_sampler_cfg:
            <<: *sampler_cfg
